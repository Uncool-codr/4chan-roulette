import os
import string
import webbrowser 
import random

board = random.choice(string.ascii_lowercase)
thing = "https://www.4chan.org/" + board + "/" 
os.system(f'start chrome --incognito "{thing}"')
